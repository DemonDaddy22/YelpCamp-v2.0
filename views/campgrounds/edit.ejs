<% layout('layouts/boilerplate.ejs') %>
<% block('head').append(`<title>Edit ${campground?.title ? campground.title : 'Campground'}</title>`) %>
    <h1>Edit campground</h1>
    <form action='/campgrounds/<%= campground.id %>?_method=PATCH' method='POST'>
        <label for='title'>Title</label>
        <input type='text' name='title' id='title' value="<%= campground?.title %>" required>
        <label for='price'>Price</label>
        <input type='numeric' name='price' id='price' value="<%= campground?.price %>" min='0' required>
        <label for='description'>Description</label>
        <input type='text' name='description' id='description' value="<%= campground?.description %>" required>
        <label for='location'>Location</label>
        <input type='text' name='location' id='location' value="<%= campground?.location %>" required>
        <button>Submit</button>
    </form>